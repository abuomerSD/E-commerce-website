const productQty = document.getElementById('input-product-qty');
const total = document.getElementById('txt-total');

productQty.addEventListener('change', (e) => {
    let totalValue = productQty.value * 1;
})

function deleteCartItem(rowId) {

}