<nav class="navbar navbar-expand-lg bg-dark" style="max-height: 19%; color: white;" data-bs-theme="dark">
   <div class="container-fluid" style="color: white;">
     <i class="fa-solid fa-shop"> </i>
     <a class="navbar-brand" href="/">Step Online Shopping</a>
     <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
       <span class="navbar-toggler-icon"></span>
     </button>
     <div class="collapse navbar-collapse" id="navbarSupportedContent">
 
       <ul class="navbar-nav me-auto mb-2 mb-lg-0">
         <li class="nav-item">
           <a class="nav-link" aria-current="page" href="/shop/bestSellers">Best Sellers</a>
         </li>
         <li class="nav-item">
           <a id ="products-nav-link" class="nav-link" href="/shop/newReleases">New Releases</a>
         </li>
         <li class="nav-item">
          <% if(categories.length > 0) {%>
            <div class="dropdown">
              <button class="nav-link" type="button" data-bs-toggle="dropdown" aria-expanded="false">
                Categories
              </button>
              <ul class="dropdown-menu">
                <% categories.forEach(category => { %>
                  <li><a class="dropdown-item" href="/shop/categories/<%= category.id %>"><%= category.name %></a></li>
                <% }); %>
              </ul>
            </div>
           </li>
          <% } %>
         <li class="nav-item">
           <a class="nav-link" href="#">Cart</a>
         </li>
       </ul>

       <form  class="d-flex" role="search" method="post" action="/shop/searchProducts">
         <input class="form-control me-2" type="search" placeholder="Search for Product" aria-label="Search" name="name">
         <button class="btn btn-outline-success" type="submit">Search</button>
        </form>
        <% if(user) { %>

         <div class="dropdown m-1">
          <button class="btn btn-secondary dropdown-toggle" type="button" data-bs-toggle="dropdown" aria-expanded="false">
            <%= user.username %>
          </button>
          <ul class="dropdown-menu">
            <li><button class="dropdown-item btn btn-danger" onclick="logout()">logout</button></li>
          </ul>
        </div>

         <div class="row ml-2">
          <i class="fa-solid fa-cart-shopping col"><p style="color: red;"></p></i>
         </div>
        <% } %>

     </div>
   </div>
 </nav>